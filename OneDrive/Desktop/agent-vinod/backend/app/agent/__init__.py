"""Stable home for agent-facing backend modules.

Phase 1 keeps the underlying implementations in their existing packages so the
app stays stable, while new imports should prefer ``app.agent.*``.
"""

